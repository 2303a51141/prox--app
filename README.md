# PopX

A small React + Vite app recreating the PopX mobile screens.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The output in `dist/` can be deployed to Vercel or Netlify with no extra configuration.
